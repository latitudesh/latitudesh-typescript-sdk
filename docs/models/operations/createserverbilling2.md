# CreateServerBilling2

The server billing type. Accepts `hourly` and `monthly` for on demand projects and `yearly` for reserved projects.

## Example Usage

```typescript
import { CreateServerBilling2 } from "latitudesh-typescript-sdk/models/operations";

let value: CreateServerBilling2 = "yearly";
```

## Values

```typescript
"hourly" | "monthly" | "yearly"
```