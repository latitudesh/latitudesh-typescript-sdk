# UpdateServerBilling2

The server billing type. Accepts `hourly` and `monthly` for on demand projects and `yearly` for reserved projects.

## Example Usage

```typescript
import { UpdateServerBilling2 } from "latitudesh-typescript-sdk/models/operations";

let value: UpdateServerBilling2 = "monthly";
```

## Values

```typescript
"hourly" | "monthly" | "yearly"
```