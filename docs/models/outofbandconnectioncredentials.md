# OutOfBandConnectionCredentials

credentials are valid only when the server is deployed with ssh keys

## Example Usage

```typescript
import { OutOfBandConnectionCredentials } from "latitudesh-typescript-sdk/models";

let value: OutOfBandConnectionCredentials = {};
```

## Fields

| Field              | Type               | Required           | Description        |
| ------------------ | ------------------ | ------------------ | ------------------ |
| `user`             | *string*           | :heavy_minus_sign: | N/A                |
| `password`         | *string*           | :heavy_minus_sign: | N/A                |