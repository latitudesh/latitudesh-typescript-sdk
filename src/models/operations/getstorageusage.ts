/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetStorageUsageRequest = {
  /**
   * Project ID or Slug
   */
  filterProject: string;
  /**
   * Restrict the result to a single storage. Accepts the storage/bucket ID
   */
  filterStorageId?: string | undefined;
  /**
   * Defaults to yesterday
   */
  filterStartDate?: RFCDate | undefined;
  /**
   * Defaults to today; clamped to today when a future date is given
   */
  filterEndDate?: RFCDate | undefined;
};

/** @internal */
export const GetStorageUsageRequest$inboundSchema: z.ZodType<
  GetStorageUsageRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  "filter[project]": z.string(),
  "filter[storage_id]": z.string().optional(),
  "filter[start_date]": z.string().transform(v => new RFCDate(v)).optional(),
  "filter[end_date]": z.string().transform(v => new RFCDate(v)).optional(),
}).transform((v) => {
  return remap$(v, {
    "filter[project]": "filterProject",
    "filter[storage_id]": "filterStorageId",
    "filter[start_date]": "filterStartDate",
    "filter[end_date]": "filterEndDate",
  });
});
/** @internal */
export type GetStorageUsageRequest$Outbound = {
  "filter[project]": string;
  "filter[storage_id]"?: string | undefined;
  "filter[start_date]"?: string | undefined;
  "filter[end_date]"?: string | undefined;
};

/** @internal */
export const GetStorageUsageRequest$outboundSchema: z.ZodType<
  GetStorageUsageRequest$Outbound,
  z.ZodTypeDef,
  GetStorageUsageRequest
> = z.object({
  filterProject: z.string(),
  filterStorageId: z.string().optional(),
  filterStartDate: z.instanceof(RFCDate).transform(v => v.toString())
    .optional(),
  filterEndDate: z.instanceof(RFCDate).transform(v => v.toString()).optional(),
}).transform((v) => {
  return remap$(v, {
    filterProject: "filter[project]",
    filterStorageId: "filter[storage_id]",
    filterStartDate: "filter[start_date]",
    filterEndDate: "filter[end_date]",
  });
});

export function getStorageUsageRequestToJSON(
  getStorageUsageRequest: GetStorageUsageRequest,
): string {
  return JSON.stringify(
    GetStorageUsageRequest$outboundSchema.parse(getStorageUsageRequest),
  );
}
export function getStorageUsageRequestFromJSON(
  jsonString: string,
): SafeParseResult<GetStorageUsageRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetStorageUsageRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetStorageUsageRequest' from JSON`,
  );
}
