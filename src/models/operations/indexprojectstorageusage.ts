/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type IndexProjectStorageUsageRequest = {
  /**
   * Project ID or Slug
   */
  projectId: string;
  /**
   * Restrict the result to a single storage. Accepts the storage/bucket ID
   */
  storageId?: string | undefined;
  /**
   * Defaults to yesterday
   */
  startDate?: RFCDate | undefined;
  /**
   * Defaults to today; clamped to today when a future date is given
   */
  endDate?: RFCDate | undefined;
};

/** @internal */
export const IndexProjectStorageUsageRequest$inboundSchema: z.ZodType<
  IndexProjectStorageUsageRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  project_id: z.string(),
  storage_id: z.string().optional(),
  start_date: z.string().transform(v => new RFCDate(v)).optional(),
  end_date: z.string().transform(v => new RFCDate(v)).optional(),
}).transform((v) => {
  return remap$(v, {
    "project_id": "projectId",
    "storage_id": "storageId",
    "start_date": "startDate",
    "end_date": "endDate",
  });
});
/** @internal */
export type IndexProjectStorageUsageRequest$Outbound = {
  project_id: string;
  storage_id?: string | undefined;
  start_date?: string | undefined;
  end_date?: string | undefined;
};

/** @internal */
export const IndexProjectStorageUsageRequest$outboundSchema: z.ZodType<
  IndexProjectStorageUsageRequest$Outbound,
  z.ZodTypeDef,
  IndexProjectStorageUsageRequest
> = z.object({
  projectId: z.string(),
  storageId: z.string().optional(),
  startDate: z.instanceof(RFCDate).transform(v => v.toString()).optional(),
  endDate: z.instanceof(RFCDate).transform(v => v.toString()).optional(),
}).transform((v) => {
  return remap$(v, {
    projectId: "project_id",
    storageId: "storage_id",
    startDate: "start_date",
    endDate: "end_date",
  });
});

export function indexProjectStorageUsageRequestToJSON(
  indexProjectStorageUsageRequest: IndexProjectStorageUsageRequest,
): string {
  return JSON.stringify(
    IndexProjectStorageUsageRequest$outboundSchema.parse(
      indexProjectStorageUsageRequest,
    ),
  );
}
export function indexProjectStorageUsageRequestFromJSON(
  jsonString: string,
): SafeParseResult<IndexProjectStorageUsageRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IndexProjectStorageUsageRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IndexProjectStorageUsageRequest' from JSON`,
  );
}
