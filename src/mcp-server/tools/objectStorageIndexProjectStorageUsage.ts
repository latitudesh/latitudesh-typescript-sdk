/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectStorageIndexProjectStorageUsage } from "../../funcs/objectStorageIndexProjectStorageUsage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.IndexProjectStorageUsageRequest$inboundSchema,
};

export const tool$objectStorageIndexProjectStorageUsage: ToolDefinition<
  typeof args
> = {
  name: "object-storage-index-project-storage-usage",
  description: `List storage usage

Returns daily object storage usage for the project. Each row reports the canonical usage in bytes for a single storage on a given day, plus the provider-reported raw value.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await objectStorageIndexProjectStorageUsage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
