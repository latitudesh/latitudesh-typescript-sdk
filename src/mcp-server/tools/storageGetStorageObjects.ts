/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageGetStorageObjects } from "../../funcs/storageGetStorageObjects.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStorageObjectsRequest$inboundSchema.optional(),
};

export const tool$storageGetStorageObjects: ToolDefinition<typeof args> = {
  name: "storage-get-storage-objects",
  description: `List object storages

Lists all object storages from a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageGetStorageObjects(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
