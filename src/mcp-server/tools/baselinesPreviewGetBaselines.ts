/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { baselinesPreviewGetBaselines } from "../../funcs/baselinesPreviewGetBaselines.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$baselinesPreviewGetBaselines: ToolDefinition = {
  name: "baselines-(preview)-get-baselines",
  description: `List baselines

**Preview.** Available to teams with the \`baselines_api\` feature flag. The shape of this
endpoint may change before general availability.

List all baselines in the team. A baseline records the configuration you expect your
servers to be delivered with — plan, SSH keys, user data, disk layout and BIOS settings.
`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await baselinesPreviewGetBaselines(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
