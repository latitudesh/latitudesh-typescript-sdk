/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamMembersDelete } from "../../funcs/teamMembersDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DestroyTeamMemberRequest$inboundSchema,
};

export const tool$teamMembersDelete: ToolDefinition<typeof args> = {
  name: "team-members-delete",
  description: `Remove a member`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamMembersDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
