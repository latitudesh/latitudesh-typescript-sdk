/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { serversScheduleDeletion } from "../../funcs/serversScheduleDeletion.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ServerScheduleDeletionRequest$inboundSchema,
};

export const tool$serversScheduleDeletion: ToolDefinition<typeof args> = {
  name: "servers-schedule-deletion",
  description: `Schedule server deletion

Schedules the server to be removed at the end of the billing cycle.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await serversScheduleDeletion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
