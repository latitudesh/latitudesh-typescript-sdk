/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storagePostStorageObjects } from "../../funcs/storagePostStorageObjects.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostStorageObjectsRequest$inboundSchema,
};

export const tool$storagePostStorageObjects: ToolDefinition<typeof args> = {
  name: "storage-post-storage-objects",
  description: `Create object storage

Creates a new object storage bucket for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storagePostStorageObjects(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
