/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lksDeleteLKSCluster } from "../../funcs/lksDeleteLKSCluster.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLksClusterRequest$inboundSchema,
};

export const tool$lksDeleteLKSCluster: ToolDefinition<typeof args> = {
  name: "lks-delete-lks-cluster",
  description: `Delete an LKS cluster

Marks the cluster for deletion. The call returns as soon as the tombstone is written; the platform then tears the cluster and its node pools down asynchronously. A cluster that is already deleted, or that is paused, rejects the request.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lksDeleteLKSCluster(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
