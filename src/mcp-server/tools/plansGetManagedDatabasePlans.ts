/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansGetManagedDatabasePlans } from "../../funcs/plansGetManagedDatabasePlans.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$plansGetManagedDatabasePlans: ToolDefinition = {
  name: "plans-get-managed-database-plans",
  description: `List managed database plans`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await plansGetManagedDatabasePlans(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
