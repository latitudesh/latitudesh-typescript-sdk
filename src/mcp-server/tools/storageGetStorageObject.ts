/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageGetStorageObject } from "../../funcs/storageGetStorageObject.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStorageObjectRequest$inboundSchema,
};

export const tool$storageGetStorageObject: ToolDefinition<typeof args> = {
  name: "storage-get-storage-object",
  description: `Retrieve object storage

Shows details of a specific object storage.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageGetStorageObject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
