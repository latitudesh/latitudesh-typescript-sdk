/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { virtualNetworksDelete } from "../../funcs/virtualNetworksDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DestroyVirtualNetworkRequest$inboundSchema,
};

export const tool$virtualNetworksDelete: ToolDefinition<typeof args> = {
  name: "virtual-networks-delete",
  description: `Delete a Virtual Network

Delete virtual network
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await virtualNetworksDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
