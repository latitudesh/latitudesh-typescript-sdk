/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { privateNetworksListAssignments } from "../../funcs/privateNetworksListAssignments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVirtualNetworksAssignmentsRequest$inboundSchema
    .optional(),
};

export const tool$privateNetworksListAssignments: ToolDefinition<typeof args> =
  {
    name: "private-networks-list-assignments",
    description: `List VLAN assignments

Returns a list of all servers assigned to virtual networks.
`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await privateNetworksListAssignments(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
