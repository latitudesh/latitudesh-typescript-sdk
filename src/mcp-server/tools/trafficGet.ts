/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trafficGet } from "../../funcs/trafficGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTrafficConsumptionRequest$inboundSchema,
};

export const tool$trafficGet: ToolDefinition<typeof args> = {
  name: "traffic-get",
  description: `Retrieve traffic`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trafficGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
