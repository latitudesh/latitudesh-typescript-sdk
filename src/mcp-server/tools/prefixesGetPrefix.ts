/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { prefixesGetPrefix } from "../../funcs/prefixesGetPrefix.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPrefixRequest$inboundSchema,
};

export const tool$prefixesGetPrefix: ToolDefinition<typeof args> = {
  name: "prefixes-get-prefix",
  description: `Retrieve a network

Retrieve a customer network`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await prefixesGetPrefix(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
