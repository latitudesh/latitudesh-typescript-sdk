/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageGetStorageVolume } from "../../funcs/storageGetStorageVolume.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStorageVolumeRequest$inboundSchema,
};

export const tool$storageGetStorageVolume: ToolDefinition<typeof args> = {
  name: "storage-get-storage-volume",
  description: `Retrieve volume

Shows details of a specific volume storage.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageGetStorageVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
