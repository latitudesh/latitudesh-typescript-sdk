/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageListFilesystems } from "../../funcs/storageListFilesystems.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStorageFilesystemsRequest$inboundSchema.optional(),
};

export const tool$storageListFilesystems: ToolDefinition<typeof args> = {
  name: "storage-list-filesystems",
  description: `List filesystems

Lists all the filesystems from a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageListFilesystems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
