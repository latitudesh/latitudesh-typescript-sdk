/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStoragePostStorageVolumesMap } from "../../funcs/blockStoragePostStorageVolumesMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostStorageVolumesMapRequest$inboundSchema,
};

export const tool$blockStoragePostStorageVolumesMap: ToolDefinition<
  typeof args
> = {
  name: "block-storage-post-storage-volumes-map",
  description: `Map volume to server

Maps a high performance volume to a server over NVMe-TCP.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await blockStoragePostStorageVolumesMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
