/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStorageMountVolume } from "../../funcs/blockStorageMountVolume.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.MountVolumeRequest$inboundSchema,
};

export const tool$blockStorageMountVolume: ToolDefinition<typeof args> = {
  name: "block-storage-mount-volume",
  description: `Mount volume (deprecated)

Mounts a volume by adding the client to an allowed list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await blockStorageMountVolume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
