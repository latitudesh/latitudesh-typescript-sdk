/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storagePostStorageVolumes } from "../../funcs/storagePostStorageVolumes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostStorageVolumesRequest$inboundSchema,
};

export const tool$storagePostStorageVolumes: ToolDefinition<typeof args> = {
  name: "storage-post-storage-volumes",
  description: `Create volume

Allows you to add persistent storage to a project. These volumes can be used to store data across your servers.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storagePostStorageVolumes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
