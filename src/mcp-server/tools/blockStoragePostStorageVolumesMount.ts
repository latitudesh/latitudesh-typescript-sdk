/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blockStoragePostStorageVolumesMount } from "../../funcs/blockStoragePostStorageVolumesMount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostStorageVolumesMountRequest$inboundSchema,
};

export const tool$blockStoragePostStorageVolumesMount: ToolDefinition<
  typeof args
> = {
  name: "block-storage-post-storage-volumes-mount",
  description: `Mount volume

Mounts a volume by adding the client to an allowed list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await blockStoragePostStorageVolumesMount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
