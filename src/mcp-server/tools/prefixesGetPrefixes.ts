/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { prefixesGetPrefixes } from "../../funcs/prefixesGetPrefixes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPrefixesRequest$inboundSchema.optional(),
};

export const tool$prefixesGetPrefixes: ToolDefinition<typeof args> = {
  name: "prefixes-get-prefixes",
  description: `List networks

List the customer networks owned by the team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await prefixesGetPrefixes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
