/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vpnSessionsList } from "../../funcs/vpnSessionsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVpnSessionsRequest$inboundSchema.optional(),
};

export const tool$vpnSessionsList: ToolDefinition<typeof args> = {
  name: "vpn-sessions-list",
  description: `List VPN sessions

List active VPN sessions.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await vpnSessionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
