/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiKeysPatchApiKey } from "../../funcs/apiKeysPatchApiKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchApiKeyRequest$inboundSchema,
};

export const tool$apiKeysPatchApiKey: ToolDefinition<typeof args> = {
  name: "api-keys-patch-api-key",
  description: `Update API key settings

Update an API key's settings (name, read_only, allowed_ips) without regenerating the token. To rotate the token, use the PUT endpoint instead.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiKeysPatchApiKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
