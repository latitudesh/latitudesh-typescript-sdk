/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { managedDatabasesDestroyManagedDatabase } from "../../funcs/managedDatabasesDestroyManagedDatabase.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DestroyManagedDatabaseRequest$inboundSchema,
};

export const tool$managedDatabasesDestroyManagedDatabase: ToolDefinition<
  typeof args
> = {
  name: "managed-databases-destroy-managed-database",
  description: `Destroy a managed database`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await managedDatabasesDestroyManagedDatabase(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
