/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageDeleteStorageObjects } from "../../funcs/storageDeleteStorageObjects.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteStorageObjectsRequest$inboundSchema,
};

export const tool$storageDeleteStorageObjects: ToolDefinition<typeof args> = {
  name: "storage-delete-storage-objects",
  description: `Delete object storage

Allows you to remove an object storage from a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageDeleteStorageObjects(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
