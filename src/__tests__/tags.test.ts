/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Tags Get Tags Success", async () => {
  const testHttpClient = createTestHTTPClient("get-tags-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.tags.list();
  expect(result).toBeDefined();
});

test("Tags Create Tag Created", async () => {
  const testHttpClient = createTestHTTPClient("create-tag-Created");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.tags.create({
    data: {
      type: "tags",
      attributes: {
        name: "Tag Name",
        description: "Tag Description",
        color: "#bebebe",
      },
    },
  });
  expect(result).toBeDefined();
});

test("Tags Update Tag Success", async () => {
  const testHttpClient = createTestHTTPClient("update-tag-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.tags.update({
    tagId: "tag_XBlke2r5RyiyVpG9LPK8tWjalLL",
    requestBody: {
      data: {
        id: "tag_XBlke2r5RyiyVpG9LPK8tWjalLL",
        type: "tags",
        attributes: {
          name: "Tag Name",
        },
      },
    },
  });
  expect(result).toBeDefined();
});
