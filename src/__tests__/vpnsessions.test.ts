/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Vpnsessions Get Vpn Sessions Success", async () => {
  const testHttpClient = createTestHTTPClient("get-vpn-sessions-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.vpnSessions.list({
    filterLocation: "SAO",
  });
  expect(result).toBeDefined();
});

test("Vpnsessions Post Vpn Session Created", async () => {
  const testHttpClient = createTestHTTPClient("post-vpn-session-Created");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.vpnSessions.create({
    data: {
      attributes: {
        site: "SAO",
        serverId: "sv_LMmAD8wyqwop2",
      },
    },
  });
  expect(result).toBeDefined();
});

test("Vpnsessions Put Vpn Session Success", async () => {
  const testHttpClient = createTestHTTPClient("put-vpn-session-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.vpnSessions.refreshPassword({
    vpnSessionId: "vpn_pRMLydp0dQKr1",
  });
  expect(result).toBeDefined();
});
