/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Userprofile Get User Profile Success", async () => {
  const testHttpClient = createTestHTTPClient("get-user-profile-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.userProfile.get();
  expect(result).toBeDefined();
});

test("Userprofile Patch User Profile Success", async () => {
  const testHttpClient = createTestHTTPClient("patch-user-profile-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.userProfile.update({
    id: "user_8PMvy2GL72uKKyXyANEnsjMQYMP",
    requestBody: {
      data: {
        id: "user_8PMvy2GL72uKKyXyANEnsjMQYMP",
        type: "users",
        attributes: {
          role: "collaborator",
        },
      },
    },
  });
  expect(result).toBeDefined();
});

test("Userprofile Get User Teams Success", async () => {
  const testHttpClient = createTestHTTPClient("get-user-teams-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.userProfile.listTeams();
  expect(result).toBeDefined();
});

test("Userprofile Get User Teams When Team Is Recent (Created Within Last Month)", async () => {
  const testHttpClient = createTestHTTPClient(
    "get-user-teams-when team is recent (created within last month)",
  );

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.userProfile.listTeams();
  expect(result).toBeDefined();
});

test("Userprofile Get User Teams When Team Is Older Than One Month", async () => {
  const testHttpClient = createTestHTTPClient(
    "get-user-teams-when team is older than one month",
  );

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.userProfile.listTeams();
  expect(result).toBeDefined();
});
