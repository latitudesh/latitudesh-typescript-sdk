/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Teams Get Team Success", async () => {
  const testHttpClient = createTestHTTPClient("get-team-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.teams.get();
  expect(result).toBeDefined();
});

test("Teams Get Team When Team Is Recent (Created Within Last Month)", async () => {
  const testHttpClient = createTestHTTPClient(
    "get-team-when team is recent (created within last month)",
  );

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.teams.get();
  expect(result).toBeDefined();
});

test("Teams Get Team When Team Is Older Than One Month", async () => {
  const testHttpClient = createTestHTTPClient(
    "get-team-when team is older than one month",
  );

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.teams.get();
  expect(result).toBeDefined();
});

test("Teams Post Team Created", async () => {
  const testHttpClient = createTestHTTPClient("post-team-Created");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.teams.create({
    data: {
      type: "teams",
      attributes: {
        name: "Name",
        currency: "USD",
        address: "Address",
      },
    },
  });
  expect(result).toBeDefined();
});

test("Teams Patch Current Team Success", async () => {
  const testHttpClient = createTestHTTPClient("patch-current-team-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.teams.update({
    teamId: "team_bVJM4y6m4VCyy101JzA3szlVGRb",
    requestBody: {
      data: {
        id: "team_bVJM4y6m4VCyy101JzA3szlVGRb",
        type: "teams",
        attributes: {
          address: "Address",
          name: "Name",
        },
      },
    },
  });
  expect(result).toBeDefined();
});
