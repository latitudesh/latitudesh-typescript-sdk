/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Ipaddresses Get Ips Success", async () => {
  const testHttpClient = createTestHTTPClient("get-ips-Success");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.ipAddresses.list({});
  expect(result).toBeDefined();
});

test("Ipaddresses Get Ip", async () => {
  const testHttpClient = createTestHTTPClient("get-ip");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.ipAddresses.get({
    ipId: "<id>",
  });
  expect(result).toBeDefined();
});
