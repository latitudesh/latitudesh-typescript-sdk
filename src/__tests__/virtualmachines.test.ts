/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Virtualmachines Create Virtual Machine Created", async () => {
  const testHttpClient = createTestHTTPClient("create-virtual-machine-Created");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.virtualMachines.create({
    data: {
      type: "virtual_machines",
      attributes: {
        name: "my-new-vm",
        project: "lightweight-leather-lamp",
      },
    },
  });
  expect(result).toBeDefined();
});
