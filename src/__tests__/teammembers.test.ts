/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Latitudesh } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Teammembers Post Team Members Created", async () => {
  const testHttpClient = createTestHTTPClient("post-team-members-Created");

  const latitudesh = new Latitudesh({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    bearer: process.env["LATITUDESH_BEARER"] ?? "value",
  });

  const result = await latitudesh.teamMembers.postTeamMembers({
    data: {
      type: "memberships",
      attributes: {
        firstName: "Maricela",
        lastName: "Torphy",
        email: "maritza_schneider@mcglynn.test",
        role: "collaborator",
      },
    },
  });
  expect(result).toBeDefined();
});
