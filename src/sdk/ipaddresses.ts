/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ipAddressesGet } from "../funcs/ipAddressesGet.js";
import { ipAddressesList } from "../funcs/ipAddressesList.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class IpAddresses extends ClientSDK {
  /**
   * List IPs
   *
   * @remarks
   * List all Management and Additional IP Addresses.
   *  • Management IPs are IPs that are used for the management IP of a device.
   *    This is a public IP address that a device is born and dies with. It never changes during the lifecycle of the device.
   *  • Additional IPs are individual IPs that can be added to a device as an additional IP that can be used.
   */
  async list(
    request?: operations.GetIpsRequest | undefined,
    options?: RequestOptions,
  ): Promise<PageIterator<operations.GetIpsResponse, { page: number }>> {
    return unwrapResultIterator(ipAddressesList(
      this,
      request,
      options,
    ));
  }

  /**
   * Retrieve an IP
   *
   * @remarks
   * Retrieve an IP Address
   */
  async get(
    request: operations.GetIpRequest,
    options?: RequestOptions,
  ): Promise<models.IpAddress> {
    return unwrapAsync(ipAddressesGet(
      this,
      request,
      options,
    ));
  }
}
